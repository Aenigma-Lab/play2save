
# AnyLink Downloader

A simple Flask-based website to download audio/video from any URL using `yt-dlp`.

## Setup

```bash
pip install -r requirements.txt
python app.py
```

## Notes

- Uses `yt-dlp` to fetch and download media.
- Temporary files are stored in `downloads/`.
- Only for personal/educational use.
